import { type Config } from "prettier";

const config: Config = {
  printWidth: 80,
  tabWidth: 2,
  semi: true,
  singleQuote: false,
  trailingComma: "all",
  bracketSpacing: true,
  arrowParens: "always",
  useTabs: false,
};

export default config;
